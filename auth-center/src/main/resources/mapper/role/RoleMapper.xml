<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jmsoftware.maf.authcenter.role.mapper.RoleMapper">
    <resultMap type="com.jmsoftware.maf.authcenter.role.entity.RolePersistence" id="RoleMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="description" column="description" jdbcType="VARCHAR"/>
        <result property="createdTime" column="created_time" jdbcType="TIMESTAMP"/>
        <result property="modifiedTime" column="modified_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="RoleMap">
        select
          id, name, description, created_time, modified_time
        from muscle_and_fitness.role
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="RoleMap">
        select
          id, name, description, created_time, modified_time
        from muscle_and_fitness.role
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="RoleMap">
        select
          id, name, description, created_time, modified_time
        from muscle_and_fitness.role
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="description != null and description != ''">
                and description = #{description}
            </if>
            <if test="createdTime != null">
                and created_time = #{createdTime}
            </if>
            <if test="modifiedTime != null">
                and modified_time = #{modifiedTime}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into muscle_and_fitness.role(name, description, created_time, modified_time)
        values (#{name}, #{description}, #{createdTime}, #{modifiedTime})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update muscle_and_fitness.role
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="description != null and description != ''">
                description = #{description},
            </if>
            <if test="createdTime != null">
                created_time = #{createdTime},
            </if>
            <if test="modifiedTime != null">
                modified_time = #{modifiedTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        DELETE FROM muscle_and_fitness.role WHERE id = #{id}
    </delete>

    <select id="selectRoleListByUserId" resultMap="RoleMap">
        SELECT role.id,
               role.name,
               role.description,
               role.created_time,
               role.modified_time
        FROM role
                 LEFT JOIN user_role ON role.id = user_role.role_id
                 LEFT JOIN user ON user_role.user_id = user.id
        WHERE user.id = #{userId}
    </select>
</mapper>
