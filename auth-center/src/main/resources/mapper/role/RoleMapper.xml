<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jmsoftware.maf.authcenter.role.mapper.RoleMapper">
    <resultMap id="BaseResultMap" type="com.jmsoftware.maf.authcenter.role.entity.RolePersistence">
        <!--@mbg.generated-->
        <!--@Table `role`-->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
        <result column="description" jdbcType="VARCHAR" property="description"/>
        <result column="deleted" jdbcType="TINYINT" property="deleted"/>
        <result column="created_time" jdbcType="TIMESTAMP" property="createdTime"/>
        <result column="modified_time" jdbcType="TIMESTAMP" property="modifiedTime"/>
    </resultMap>

    <select id="selectRoleListByUserId" resultMap="BaseResultMap">
        SELECT role.id,
               role.name,
               role.description,
               role.created_time,
               role.modified_time
        FROM role
                     LEFT JOIN user_role ON role.id = user_role.role_id
                     LEFT JOIN user ON user_role.user_id = user.id
        WHERE user.id = #{userId}
    </select>

    <select id="selectByName" resultMap="BaseResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        from role
        WHERE role.name = #{roleName}
    </select>
</mapper>
